<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/stop_nav_graph"
    app:startDestination="@id/stop_list_fragment">

    <fragment
        android:id="@+id/stop_list_fragment"
        android:name="com.chaeny.busoda.stoplist.StopListFragment"
        tools:layout="@layout/fragment_stop_list">
        <action
            android:id="@+id/action_stop_list_fragment_to_stop_detail_fragment"
            app:destination="@id/stop_detail_fragment" />
    </fragment>

    <fragment
        android:id="@+id/stop_detail_fragment"
        android:name="com.chaeny.busoda.stopdetail.StopDetailFragment"
        tools:layout="@layout/fragment_stop_detail">
    </fragment>

</navigation>